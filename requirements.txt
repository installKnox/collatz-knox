# Core dependencies
matplotlib>=3.9.0
numpy>=2.0.0

# Database
sqlite3-binary; platform_system == "Windows"

# Optional: progress bars, logging helpers (future-proof)
tqdm>=4.66.0
loguru>=0.7.0

# Dev/test tools
pytest>=8.2.0
